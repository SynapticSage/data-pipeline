function ry_fixFolder(dayDir, varargin)
% Fixes common issues with trodesComments or with missing stateScriptLog files (e.g. in first sleep sessions)
if isempty(opt.problemFiles)
    [~, opt.problemFiles] = 
end
